// ternary or conditional operator

let number = -1;
let finalNumber = number > 0 ? "POSITIVE NUMBER" : "NEGATIVE NUMBER";
console.log(finalNumber);


let age = 10;
let drink;
if(age>=18){
    drink = "Coffee";
    console.log(`Prefer ${drink} bcz age is greater than 18`);
}else{
    drink = "Milk";
}
console.log(`Prefer ${drink} bcz age is less than 18`);

