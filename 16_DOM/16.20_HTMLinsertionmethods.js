//  document.createElment
// appendChild method -> to add a child element to any element inside body tag
// node.append(e) method -> append at the end of node
// node.prepend(e) method -> insert at the beginning of node
// node.before(e) -> insert before node
// node.after(e) -> insert after node
// node.replaceWith(e) -> replaces node with the given node.

